import $ from 'jquery';
import _ from 'underscore';
import URI from 'urijs';
import Backbone from 'backbone';

import './docparser';

import './rgpercent';

window._ = _;
window.$ = $;
window.URI = URI;
window.Backbone = Backbone;
